{
  "opencollection": "1.0.0",
  "info": {
    "name": "DigitalOcean Tags API",
    "version": "2.0"
  },
  "request": {
    "auth": {
      "type": "bearer",
      "token": "{{bearerToken}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Tags",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "List All Tags",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://api.digitalocean.com/v2/tags",
            "params": [
              {
                "name": "per_page",
                "value": "2",
                "type": "query",
                "description": "Number of items returned per page"
              },
              {
                "name": "page",
                "value": "1",
                "type": "query",
                "description": "Which 'page' of paginated results to return."
              }
            ],
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "To list all of your tags, you can send a GET request to `/v2/tags`."
        },
        {
          "info": {
            "name": "Create a New Tag",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://api.digitalocean.com/v2/tags",
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "To create a tag you can send a POST request to `/v2/tags` with a `name` attribute."
        },
        {
          "info": {
            "name": "Retrieve a Tag",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://api.digitalocean.com/v2/tags/:tag_id",
            "params": [
              {
                "name": "tag_id",
                "value": "awesome",
                "type": "path",
                "description": "The name of the tag. Tags may contain letters, numbers, colons, dashes, and underscores. There is a limit of 255 characters per tag."
              }
            ],
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "To retrieve an individual tag, you can send a `GET` request to `/v2/tags/$TAG_NAME`."
        },
        {
          "info": {
            "name": "Delete a Tag",
            "type": "http"
          },
          "http": {
            "method": "DELETE",
            "url": "https://api.digitalocean.com/v2/tags/:tag_id",
            "params": [
              {
                "name": "tag_id",
                "value": "awesome",
                "type": "path",
                "description": "The name of the tag. Tags may contain letters, numbers, colons, dashes, and underscores. There is a limit of 255 characters per tag."
              }
            ],
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "A tag can be deleted by sending a `DELETE` request to `/v2/tags/$TAG_NAME`. Deleting a tag also untags all the resources that have previously been tagged by the Tag"
        },
        {
          "info": {
            "name": "Tag a Resource",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://api.digitalocean.com/v2/tags/:tag_id/resources",
            "params": [
              {
                "name": "tag_id",
                "value": "awesome",
                "type": "path",
                "description": "The name of the tag. Tags may contain letters, numbers, colons, dashes, and underscores. There is a limit of 255 characters per tag."
              }
            ],
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "Resources can be tagged by sending a POST request to `/v2/tags/$TAG_NAME/resources` with an array of json objects containing `resource_id` and `resource_type` attributes.\nCurrently only tagging of Droplets, Databases, Images, Volumes, and Volume Snapshots is supported. `resource_type` is expected to be the string `droplet`, `database`, `image`, `volume` or `volume_snapshot`. `resource_id` is expected to be the ID of the resource as a string."
        },
        {
          "info": {
            "name": "Untag a Resource",
            "type": "http"
          },
          "http": {
            "method": "DELETE",
            "url": "https://api.digitalocean.com/v2/tags/:tag_id/resources",
            "params": [
              {
                "name": "tag_id",
                "value": "awesome",
                "type": "path",
                "description": "The name of the tag. Tags may contain letters, numbers, colons, dashes, and underscores. There is a limit of 255 characters per tag."
              }
            ],
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "Resources can be untagged by sending a DELETE request to `/v2/tags/$TAG_NAME/resources` with an array of json objects containing `resource_id` and `resource_type` attributes.\nCurrently only untagging of Droplets, Databases, Images, Volumes, and Volume Snapshots is supported. `resource_type` is expected to be the string `droplet`, `database`, `image`, `volume` or `volume_snapshot`. `resource_id` is expected to be the ID of the resource as a string."
        }
      ]
    }
  ],
  "bundled": true
}